# arc-example-login-flow using Deno 

Example of `.arc` defined HTTP functions for `Content-Type: text/html`.

## Usage

    npm start

Then visit the URL presented and use 'admin@example.com' and 'admin' as the email and password.
